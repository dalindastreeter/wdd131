const hamburger = document.quarySelector('nav');
const navList = document.querySelector('#menu');

hamburger.addEventListener('click', () => {
    mainmav.classList.toggle('active');
    hambutton.classList.toggle('active');
});